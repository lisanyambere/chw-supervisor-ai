#!/usr/bin/env bash
# Wipe OpenMRS data volumes and reseed — use to get a clean demo state
set -e

echo "Reset script — not yet implemented."
echo "This will: stop containers, drop volumes, restart, reseed."
